//! Local Noxis node executable.

fn main() {
    println!(
        "Noxis local-node library is available. Supply a validated NodeConfig and audited verifier/policy through the embedded API; no network listener is enabled."
    );
}
